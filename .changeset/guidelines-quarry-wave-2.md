---
"rewrite-rs-skills": minor
---

Add three skills — `rust-macros`, `rust-serde`, and `rust-ffi` — rewrite `rust-errors` around the public struct error shape, enrich the existing skill set with naming, dependency injection, numeric safety, the two mocking patterns, and the generated-code review smells, and gate every untagged Rust example on compiling in CI.
