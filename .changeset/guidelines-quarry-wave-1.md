---
"rewrite-rs-skills": minor
---

Add four Rust skills — `rust-performance`, `rust-concurrency`, `rust-observability`, and `rust-docs` — extend `setup-rust-skills` with concrete lint tables and a tooling set, and adopt a fence convention that separates complete Rust examples from deliberate fragments.
